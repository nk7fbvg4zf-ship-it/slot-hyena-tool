# Professional Ver.9.4
機種詳細の「設定判別」タブが押せない問題を修正。

原因:
Ver.9.3で設定判別タブを誤って `proOpenSettingSelect()` に接続し、
その関数内から存在しない `show()` を呼んでいたためクリック時に停止していました。

修正:
- 機種詳細: 狙い目 → 設定判別 を同じ機種のまま切替
- 設定判別 → 狙い目 も同じ機種のまま戻る
- 壊れた proOpenSettingSelect/show 呼び出しを撤去
- Ver.9.2までの空白画面修正と52機種カウンターは維持
